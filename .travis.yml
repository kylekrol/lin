matrix:
    include:
        - language: cpp
          sudo: false
          compiler: clang
          script:
              - mkdir build && cd build
              - cmake ../
              - cmake --build .
              - ctest --verbose .
